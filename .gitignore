/node_modules
/dist
/.idea
.env
.env.local
.env.production
ADMININFO.txt
cookies.txt
*.log
.DS_Store
user_cookies.txt
admin_cookies.txt

# Guide PDFs (exclude actual files but keep directory structure)
# Uncomment to exclude PDFs from git (use persistent disk instead)
# /guides/full/*.pdf
!/guides/full/.gitkeep
